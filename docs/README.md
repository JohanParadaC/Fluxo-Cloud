# docs

`screenshot.png` es la captura que abre el README de la raíz.

Ahora mismo hay un **placeholder**: un marco discontinuo sobre el fondo del sitio.
Sustituyelo por la captura real manteniendo el nombre, para que el enlace del README
siga funcionando sin tocar nada.

- Tamaño de referencia: **1200 × 630** (misma proporción que la `og:image` pendiente).
- Formato: PNG.
- Lo que mejor se ve en 30 segundos es el hero completo, con el menú y el panel animado.
